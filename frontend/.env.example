# Optional analytics — leave blank to disable. Values are baked in at build time.
VITE_GA_ID=
VITE_PLAUSIBLE_DOMAIN=

# Dev only: where the Vite proxy forwards /api
VITE_API_TARGET=http://localhost:8080
